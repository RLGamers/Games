2PGame Class
* Defines a set of Rules:
    * For now, all games are alternating players
    * GameState: What the board looks like now (e.g. 2D array for chess)
    * Action: An action on GameState
        * isValidAction(Action) method
    * Agent1
    * Agent2
    * On each turn, calls one of the Agent's getTurn() methods, and then posts the result of the turn to all agents

Agent Class
* Can remember history of actions through post method
* getTurn(GameState) : Action
* postTurn(GameState, Action, ) : void